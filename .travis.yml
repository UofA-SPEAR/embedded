git:
    submodules: true

matrix:
    include:
        - name: "Build"
          language: cpp
          os: linux
          addons:
              apt:
                  packages: gcc-arm-none-eabi
          language: c++
          before_script:
            - sudo apt-get install gcc-arm-none-eabi
          script:
            - "cd arm_controller/ && make"


notifications:
    email: false
