# List of all the board related files.
BOARDSRC = $(COMMON)/SPEAR_SPEEDY_F3/board.c

# Required include directories
BOARDINC = $(COMMON)/SPEAR_SPEEDY_F3

# Shared variables
ALLCSRC += $(BOARDSRC)
ALLINC  += $(BOARDINC)
